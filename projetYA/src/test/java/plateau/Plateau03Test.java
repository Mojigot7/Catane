package plateau;

import static org.junit.jupiter.api.Assertions.*;

import java.awt.Color;

import org.junit.jupiter.api.Test;

import joueur.Humain;
import joueur.Joueur;

class Plateau03Test {

	/*@Test
	void estSurTuileDuVoleur() {
		Plateau p = new Plateau();
		Joueur j1 = new Humain("J",Color.RED);
		Joueur j2 = new Humain("K",Color.BLUE);
		Joueur j3 = new Humain("L",Color.GREEN);
		p.deplacerVoleur(1, 3, j1);
		p.poserColonie(2, 2, j2);
		p.poserColonie(0, 4, j3);
		j2.getInventaire().getRessource().replace("BOIS", 1);
		j3.getInventaire().getRessource().replace("ARGILE", 1);
		p.affiche();
		p.estSurTuileDuVoleur(j1);
		assertEquals(0,j2.getInventaire().getRessource().get("BOIS"));
		assertEquals(0,j3.getInventaire().getRessource().get("BOIS"));
		assertEquals(1,j1.getInventaire().getRessource().get("BOIS"));
		assertEquals(1,j1.getInventaire().getRessource().get("ARGILE"));
	}*/

}